<% @books.each do |book| %>

  <h2>
    <%= link_to book.title, book_path(book) %>
    <!-- <a href="/books/<%= book.id %>"><%= book.title %></a>
    <%= books_path %>
    <%= book_path(book) %>
    <%= link_to book.title, book_path(book) %> -->

  </h2>
  <hr>

<% end %>
